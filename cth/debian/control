Source: cth-cluster
Section: local
Priority: optional
Maintainer: dshuman <dshuman@health.usf.edu>
Build-Depends: debhelper (>= 8.0.0), autotools-dev,
   gawk,
   qtbase5-dev,
   octave-pkg-dev (>= 1.4.4),
   cdbs, 
   qt5-default,
   pkg-config,
   libglib2.0-0,
   libpopt0,
   perl-modules,
   qtcreator,
   qt5-doc,
   qt5-doc-html,
   octave (>= 4.2.0),
   octave-common (>= 4.2.0),
   octave-general (>= 2.0.0),
   octave-geometry (>= 3.0.0),
   octave-io (>= 2.4.7),
   octave-linear-algebra (>= 2.2.2),
   octave-sockets (>= 1.2.0),
   octave-statistics (>= 1.3.0),
   octave-struct (>= 1.0.14),
   libfreexl-dev,
   libfltk1.3,
   liblapack-dev,
   wmctrl,
   r-base,
   r-cran-rcpp,
   xdotool,
   libboost-program-options-dev
Standards-Version: 4.2.1
Homepage: cisc3

Package: cth-cluster
Architecture: amd64
Depends: ${shlibs:Depends}, ${misc:Depends},
   octave (>= 4.2.0),
   octave-common (>= 4.2.0),
   octave-io (>= 2.4.7),
   octave-general (>= 2.0.0),
   octave-geometry (>= 3.0.0),
   octave-linear-algebra (>= 2.2.2),
   octave-sockets (>= 1.2.0),
   octave-statistics (>= 1.3.0),
   octave-struct (>= 1.0.14),
   octave-cth (>= 1.3.10),
   libfltk1.3,
   wmctrl,
   r-base,
   xdotool,
   perl (>= 5.0),
   libspreadsheet-writeexcel-perl,
   libspreadsheet-parseexcel-perl
Recommends:
Suggests:
   cth-curves
Description: Some cth clustering analysis tools
 .
 The programs in this package are part of the lab's CTH cluster analysis
 package.  The cth_cluster program reads one or more .adt, .bdt, or .edt files
 and caculates a variety of data from spike channels.  The information is
 written to an octave-compatible text file. There are octave .m programs that
 read this in and present it in various forms.  The octave programs can be
 executed in a terminal or from a GUI.  The cthgui program that is part of this
 package presents the GUI.  The octave programs are available in the octave-cth
 package.
